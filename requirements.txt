# ---------------------------------------------------------------------------
# J.A.R.V.I.S. - Dependencias
# Instalar con:  pip install -r requirements.txt
# ---------------------------------------------------------------------------

# --- Interfaz grafica (obligatorio) ---
PyQt6>=6.6.0

# --- API de Claude (obligatorio si usas Claude como cerebro) ---
# La clave NO va aqui ni en el codigo: va en un archivo .env.
anthropic>=0.69.0

# --- Conexion con Ollama / peticiones HTTP (obligatorio) ---
requests>=2.31.0

# --- Informacion del sistema: CPU, RAM, bateria (obligatorio) ---
psutil>=5.9.0

# --- Texto a voz (opcional, pero recomendado) ---
pyttsx3>=2.90

# --- Reconocimiento de voz (opcional) ---
SpeechRecognition>=3.10.0
# PyAudio es necesario para usar el microfono.
# En Windows normalmente basta con "pip install pyaudio".
# Si falla, revisa el README (seccion "Problemas comunes").
PyAudio>=0.2.13

# --- Control de volumen en Windows (opcional) ---
pycaw>=20230407
comtypes>=1.2.0

# --- Control de brillo de pantalla (opcional) ---
screen-brightness-control>=0.22.0
